/* ============================================================
   DroneTV — Bootstrap
   Loaded last (deferred) — mounts the initial view.
   ============================================================ */
window.DroneTV.showView('home');
